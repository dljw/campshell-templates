{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["target"],
  "properties": {
    "target": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200,
      "description": "Domain name (e.g. example.com) or webpage URL (with https://www.) to get ranked keywords for"
    },
    "locationCode": {
      "type": "integer",
      "description": "Search engine location code. Default: 2840 (United States)",
      "default": 2840
    },
    "languageCode": {
      "type": "string",
      "description": "Language code (e.g. en). Default: en",
      "default": "en"
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 1000,
      "description": "Maximum number of keywords to return. Default: 100",
      "default": 100
    },
    "itemTypes": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["organic", "paid", "featured_snippet", "local_pack", "ai_overview_reference"]
      },
      "description": "SERP item types to include. Default: [organic, paid]"
    }
  },
  "additionalProperties": false
}
