{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "seo-tracker/keywords.schema.json",
  "type": "object",
  "required": [
    "id",
    "createdAt",
    "term"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9-]{2,36}$"
    },
    "createdAt": {
      "type": "string",
      "format": "date-time"
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time"
    },
    "term": {
      "type": "string",
      "maxLength": 200,
      "description": "The keyword or search phrase"
    },
    "pageId": {
      "type": "string",
      "pattern": "^[a-z0-9-]{2,36}$",
      "description": "ID of a pages record"
    },
    "position": {
      "type": "integer",
      "maximum": 999,
      "minimum": 0,
      "description": "Current SERP ranking position (0 = not ranked)"
    },
    "previousPosition": {
      "type": "integer",
      "maximum": 999,
      "minimum": 0,
      "description": "Previous ranking position for tracking changes"
    },
    "searchVolume": {
      "type": "integer",
      "minimum": 0,
      "description": "Estimated monthly search volume"
    },
    "difficulty": {
      "type": "integer",
      "maximum": 100,
      "minimum": 0,
      "description": "Keyword difficulty score (0-100)"
    },
    "intent": {
      "type": "string",
      "enum": [
        "informational",
        "navigational",
        "commercial",
        "transactional"
      ]
    },
    "status": {
      "type": "string",
      "enum": [
        "tracking",
        "paused",
        "achieved"
      ]
    },
    "notes": {
      "type": "string"
    },
    "domainId": {
      "type": "string",
      "pattern": "^[a-z0-9-]{2,36}$",
      "description": "Reference to domain"
    }
  },
  "additionalProperties": false,
  "_generated": "7df35d8a1e880384"
}