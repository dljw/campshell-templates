{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "seo-tracker/issues.schema.json",
  "type": "object",
  "required": [
    "id",
    "createdAt",
    "title"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9-]{2,36}$"
    },
    "createdAt": {
      "type": "string",
      "format": "date-time"
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time"
    },
    "title": {
      "type": "string",
      "maxLength": 300
    },
    "issueType": {
      "type": "string",
      "enum": [
        "speed",
        "mobile",
        "indexing",
        "structure",
        "security",
        "other"
      ]
    },
    "affectedPage": {
      "type": "string",
      "format": "uri",
      "description": "URL of the affected page"
    },
    "description": {
      "type": "string",
      "maxLength": 2000
    },
    "priority": {
      "type": "string",
      "enum": [
        "critical",
        "high",
        "medium",
        "low"
      ]
    },
    "status": {
      "type": "string",
      "enum": [
        "open",
        "in-progress",
        "resolved"
      ]
    },
    "domainId": {
      "type": "string",
      "pattern": "^[a-z0-9-]{2,36}$",
      "description": "Reference to domain"
    }
  },
  "additionalProperties": false,
  "_generated": "af7bcfa02495adcd"
}