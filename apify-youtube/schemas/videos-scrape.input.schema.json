{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "apify-youtube/videos-scrape.input.schema.json",
  "type": "object",
  "required": ["channelUrl"],
  "properties": {
    "channelUrl": {
      "type": "string",
      "format": "uri",
      "minLength": 1,
      "description": "YouTube channel URL"
    },
    "maxVideos": {
      "type": "integer",
      "minimum": 1,
      "maximum": 200,
      "default": 20,
      "description": "Max number of videos to fetch (1-200)"
    },
    "onlyPostsNewerThan": {
      "type": "string",
      "format": "date",
      "description": "Only return videos published on or after this date (YYYY-MM-DD). Optional."
    },
    "onlyPostsOlderThan": {
      "type": "string",
      "format": "date",
      "description": "Only return videos published on or before this date (YYYY-MM-DD). Optional."
    },
    "sortBy": {
      "type": "string",
      "enum": ["newest", "popular", "oldest"],
      "default": "newest",
      "description": "Sort order for the videos returned"
    },
    "proxyCountry": {
      "type": "string",
      "enum": ["", "US", "GB", "DE", "FR", "JP", "BR", "IN", "AU", "CA"],
      "default": "",
      "description": "Apify proxy country (empty = automatic)"
    },
    "cacheMedia": {
      "type": "boolean",
      "default": true,
      "description": "Download video thumbnails to local cache. Recommended on — YouTube thumbnails are tiny."
    }
  },
  "additionalProperties": false
}
