{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "apify-youtube/channel-scrape.input.schema.json",
  "type": "object",
  "required": ["channelUrls"],
  "properties": {
    "channelUrls": {
      "type": "array",
      "items": { "type": "string", "format": "uri", "minLength": 1 },
      "minItems": 1,
      "maxItems": 50,
      "description": "YouTube channel URLs (max 50 per call)"
    },
    "proxyCountry": {
      "type": "string",
      "enum": ["", "US", "GB", "DE", "FR", "JP", "BR", "IN", "AU", "CA"],
      "default": "",
      "description": "Apify proxy country (empty = automatic)"
    }
  },
  "additionalProperties": false
}
