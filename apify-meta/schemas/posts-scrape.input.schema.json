{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "apify-meta/posts-scrape.input.schema.json",
  "type": "object",
  "required": ["pageUrl"],
  "properties": {
    "pageUrl": {
      "type": "string",
      "format": "uri",
      "minLength": 1,
      "description": "Facebook page URL"
    },
    "postsLimit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 200,
      "default": 20,
      "description": "Max number of posts to fetch (1-200)"
    },
    "onlyPostsNewerThan": {
      "type": "string",
      "format": "date",
      "description": "Only return posts published on or after this date (YYYY-MM-DD). Optional."
    },
    "onlyPostsOlderThan": {
      "type": "string",
      "format": "date",
      "description": "Only return posts published on or before this date (YYYY-MM-DD). Optional."
    },
    "proxyCountry": {
      "type": "string",
      "enum": ["", "US", "GB", "DE", "FR", "JP", "BR", "IN", "AU", "CA"],
      "default": "",
      "description": "Apify proxy country (empty = automatic)"
    },
    "cacheMedia": {
      "type": "boolean",
      "default": false,
      "description": "Download post thumbnails to local cache so historical runs render after Apify URLs expire."
    }
  },
  "additionalProperties": false
}
