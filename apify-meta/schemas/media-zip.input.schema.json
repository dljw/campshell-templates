{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "apify-meta/media-zip.input.schema.json",
  "type": "object",
  "required": ["runId"],
  "properties": {
    "runId": {
      "type": "string",
      "description": "ID of a previous run whose cached media should be bundled."
    },
    "itemIds": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Optional subset — only bundle media for items with these IDs."
    }
  },
  "additionalProperties": false
}
